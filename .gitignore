# Current-build zips at the top level are transient; permanent copies live
# in releases/vX.Y.Z/ (which IS tracked and never deleted).
/MRO-*.zip

# Wine/compiler droppings
*.log

# Native build outputs (CI artifacts, not sources)
/native/build/
/native/install/
